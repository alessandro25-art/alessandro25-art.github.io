# Ralph Progress Log
Started: 2026-03-24
---

## US-001
- Added `position: relative` to `.card-thumb` rule
- Updated `.card-thumb img` to use `position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; display: block`
- Files changed: index.html
- Learnings:
  - `height: 100%` on an img inside an aspect-ratio container is unreliable; `position: absolute; inset: 0` is the correct cross-browser fix
---

## US-001
- Changed `.card` flex basis from 440px to 616px (40% increase)
- Files changed: index.html
- Learnings:
  - Simple single-property CSS change; no side effects on layout since cards are inside a flex carousel
---

## US-002
- Removed `scroll-snap-type: x mandatory` from `.work-carousel`
- Added IIFE script before `</body>`: clones all `.card` elements, appends clones to carousel, runs RAF loop incrementing `scrollLeft` by 0.6/frame, resets when `>= halfWidth` for seamless loop, pauses on mouseenter/touchstart, resumes on mouseleave/touchend
- Files changed: index.html
- Learnings:
  - `halfWidth` must be computed after cloning so it reflects the full doubled scrollWidth
  - Removed scroll-snap to prevent snap fighting the programmatic scroll
---

## Codebase Patterns
- index.html is self-contained: all CSS in <style> block, all JS before </body>
- Use Edit tool only — never Write the whole file (too large, too much context loss)
- No build step, no typecheck, no tests — quality check is manual code review of the changed block
- git remote does not exist — local commits only
---
