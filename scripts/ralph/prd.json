{
  "project": "Portfolio",
  "description": "Fix hero-right vertical alignment on desktop",
  "userStories": [
    {
      "id": "US-001",
      "title": "Fix: hero-right vertical alignment on desktop",
      "description": "On desktop the right column (.hero-right) is only as tall as its content, so justify-content:center has no effect. The fix is to change align-items on .section-hero from flex-start to stretch so both columns share the same height. The mobile override (flex-direction:column at max-width:600px) must remain untouched.",
      "acceptanceCriteria": [
        "In .section-hero CSS rule, change `align-items: flex-start` to `align-items: stretch`",
        "No other CSS or HTML is changed",
        "The mobile @media (max-width: 600px) block is not modified"
      ],
      "priority": 1,
      "passes": false
    }
  ]
}
